# Core Framework
Flask>=3.0.0,<4.0.0

# Security (included with Flask but specified for clarity)
Werkzeug>=3.0.0,<4.0.0

# Excel Export
openpyxl>=3.1.0,<4.0.0

# Additional useful packages (optional but recommended)
# For better development experience
python-dotenv>=1.0.0,<2.0.0

# Note: sqlite3 is part of Python standard library and doesn't need to be installed
